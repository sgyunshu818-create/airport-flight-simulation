spring:
  datasource:
    url: jdbc:h2:mem:airport_simulation_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=CURRENT_TIME;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
