# 机场航班运行仿真系统优化总结

## 一、优化背景

本次优化围绕“将课程级机场航班仿真系统升级为更接近商业机场运行平台的前端与功能体验”展开。优化方向参考了机场运行控制中心、AODB、资源管理系统、A-CDM 协同决策平台等同类产品的常见界面风格和功能组织方式。

优化目标包括：

1. 保留原有航班、跑道、停机位、仿真、日志、统计等核心功能。
2. 将普通后台界面升级为机场运行指挥中心风格。
3. 提高运行态势展示、资源状态展示和仿真控制的可读性。
4. 为后续资源冲突检测、异常处置、甘特图排班、场景对比等商业化功能扩展打基础。

## 二、已完成的主要优化

### 1. 整体 UI 重构

已将原有顶部导航式后台重构为“左侧固定导航 + 顶部运行态势栏 + 主工作区”的机场运控平台布局。

主要改动：

1. 新增左侧导航栏，包含运行概览、航班调度、跑道资源、停机位、仿真控制、事件日志、统计分析等入口。
2. 新增顶部运行态势栏，展示天气、风向风速、能见度、QNH、流量等级、运行告警和用户信息。
3. 统一深色科技风视觉语言，使用青蓝色数据强调、琥珀色预警、红色严重告警、绿色可用状态。
4. 优化按钮、表格、指标卡、状态标签、筛选栏、表单等基础组件样式。
5. 增加响应式适配，保证小屏幕下导航、表格和表单仍可访问。

涉及文件：

1. `src/main/resources/templates/fragments/header.html`
2. `src/main/resources/static/css/app.css`
3. `src/main/resources/static/js/app.js`

### 2. 运行概览升级

运行概览已从简单统计页升级为机场运行主屏。

新增内容：

1. 今日航班、进港航班、出港航班、延误航班、在场航空器、运行评分等指标卡。
2. 航班运行时间轴，用于展示航班计划、进度和状态。
3. 延误队列，用于展示等待或延误航班。
4. 仿真任务状态，用于展示当前仿真任务的运行情况。
5. 资源利用率，用于展示跑道、停机位、完成率和延误压力。
6. 快捷操作区，用于进入单步推进、自动运行、新增航班和事件查看。
7. 事件日志区，用于展示最近仿真事件。

涉及文件：

1. `src/main/resources/templates/dashboard.html`
2. `src/main/java/com/example/airportsimulation/controller/DashboardController.java`

### 3. 机场运行态势图

新增并升级了机场运行态势图，使其更接近机场运行控制中心中的 airside 视图。

当前能力：

1. 展示双跑道结构：36L/18R 与 36R/18L。
2. 展示跑道可用状态。
3. 展示滑行道主干与分支。
4. 展示停机位矩阵。
5. 区分可用、滑行/占用、拥堵状态。
6. 展示航班标牌，如 CA1357、MU5532、CZ3456。
7. 提供状态图例。
8. 在没有真实机位数据时展示完整示例布局，避免页面空白。

涉及页面：

1. 运行概览：`/dashboard`
2. 仿真控制：`/simulation`

### 4. 仿真控制页升级

仿真控制页已从普通任务表单升级为控制台式页面。

新增内容：

1. 航班、跑道、停机位、等待/延误、任务状态、最新事件指标。
2. 当前任务卡，展示任务名称、开始时间、当前时间、结束时间、时间步长和状态。
3. 单步推进、自动运行、重置仿真操作。
4. 创建仿真任务表单。
5. 等待与延误队列。
6. 资源态势图。
7. 跑道状态、停机位状态、最近仿真日志。

涉及文件：

1. `src/main/resources/templates/simulation.html`
2. `src/main/java/com/example/airportsimulation/controller/SimulationController.java`

### 5. 数据管理页面统一

已统一航班、跑道、停机位、事件日志、统计分析等页面的视觉结构。

优化内容：

1. 表格统一为深色专业数据表。
2. 列表增加条数标识。
3. 删除操作增加确认提示。
4. 统计页面增加资源利用率进度条。
5. 表单页面保留原有业务字段并融入新视觉系统。

涉及页面：

1. `/flights`
2. `/runways`
3. `/gates`
4. `/records`
5. `/statistics`

### 6. 验证结果

已完成以下验证：

1. `mvn test` 通过，测试数量 30，失败 0，错误 0。
2. 本地服务已成功启动。
3. 主要页面均返回 200。
4. 已使用 Playwright 截图检查运行概览页面。
5. 最新截图路径：`target/dashboard-redesign.png`

## 三、当前系统与商业产品的差距

相比市面上的机场运营管理、AODB、资源管理和 A-CDM 产品，当前系统仍需要补充以下能力：

1. 资源冲突检测与自动推荐。
2. 异常事件处置闭环。
3. 资源甘特图和排班视图。
4. 航班计划导入、仿真结果导出和报表输出。
5. 多场景仿真与方案对比。
6. 多角色权限和差异化工作台。
7. 更细粒度的航班里程碑和协同决策字段。

## 四、下一阶段优化计划

建议按以下顺序继续扩展：

1. 资源冲突检测与推荐：识别跑道、停机位、机型和时间占用冲突，并给出推荐资源。
2. 异常事件处置中心：将事件从日志升级为可处理、可跟踪、可闭环的异常工单。
3. 资源甘特图：按时间轴展示跑道和停机位的航班占用情况。
4. 导入导出：支持航班计划 CSV 导入、事件日志和统计结果 CSV 导出。
5. 多场景仿真：支持日常运行、雷雨保障、大流量保障、跑道关闭等场景，并进行方案对比。
6. 角色权限：区分管理员、运控人员、调度员、资源管理员、观察者等角色。

## 五、总结

本次优化已完成从普通后台到机场运行指挥平台的第一阶段升级。系统目前具备更强的商业展示能力和更清晰的运行态势表达。后续应重点补齐资源智能推荐、异常处置闭环和排班可视化能力，使系统从“仿真管理工具”进一步升级为“机场运行决策支持平台”。

## 六、本轮新增商业化功能

在 UI 优化完成后，系统继续补充了面向同类商业产品的运行增强功能。

### 1. 资源冲突检测与自动推荐

新增页面：`/operations/conflicts`

新增能力：

1. 检测等待航班、延误航班、跑道占用异常、机位占用异常。
2. 检测到达航班是否存在适配机位不足。
3. 检测出港航班是否存在起飞跑道不足。
4. 根据航班类型、机型、资源空闲状态和优先级给出推荐资源。
5. 将冲突等级划分为预警和严重。

### 2. 异常事件处置中心

新增页面：`/operations/incidents`

新增能力：

1. 将仿真日志转化为事件处置清单。
2. 为事件标注提示、通知、告警等级。
3. 增加处理状态字段。
4. 根据事件类型生成推荐处置动作。
5. 支持从处置中心跳转原始日志。

### 3. 资源甘特图与排班视图

新增页面：`/operations/schedule`

新增能力：

1. 按时间轴展示航班计划、跑道资源、停机位资源。
2. 使用甘特条表达资源占用窗口。
3. 通过颜色区分正常、空闲、占用、延误等状态。
4. 为后续拖拽排班和冲突调整预留界面基础。

### 4. 数据导入导出

新增页面：`/operations/data`

新增能力：

1. 支持 CSV 导入航班计划。
2. 支持导出仿真事件日志 CSV。
3. 支持导出统计结果 CSV。
4. CSV 航班导入字段为：`flightNo,airline,departureAirport,arrivalAirport,flightType,aircraftType,plannedTime`。

### 5. 多场景方案对比

新增页面：`/operations/scenarios`

新增能力：

1. 提供日常运行、雷雨保障、大流量保障、跑道关闭四类场景。
2. 对比完成率、延误率、跑道占用率。
3. 为不同场景生成运行建议。

### 6. 角色权限视图

新增页面：`/operations/roles`

新增能力：

1. 定义管理员、运控人员、航班调度员、资源管理员、观察者五类角色。
2. 展示每类角色的可见模块。
3. 展示每类角色的权限范围。

当前版本已完成角色权限的功能边界设计和页面展示，尚未接入真实登录鉴权拦截。

### 7. 新增代码位置

主要新增文件：

1. `src/main/java/com/example/airportsimulation/service/OperationsInsightService.java`
2. `src/main/java/com/example/airportsimulation/controller/OperationsController.java`
3. `src/main/resources/templates/operations-conflicts.html`
4. `src/main/resources/templates/operations-incidents.html`
5. `src/main/resources/templates/operations-schedule.html`
6. `src/main/resources/templates/operations-data.html`
7. `src/main/resources/templates/operations-scenarios.html`
8. `src/main/resources/templates/operations-roles.html`

同时更新：

1. `src/main/resources/templates/fragments/header.html`
2. `src/main/resources/static/css/app.css`
